---
# Secrets file for Kubernetes cluster setup
# DO NOT commit this file to version control!
# Add this file to .gitignore

# Kubernetes cluster secrets
k8s_cluster_name: "my-k8s-cluster"

# kubeadm token (will be auto-generated if not specified)
# kubeadm_token: ""

# Certificate key for control plane join (will be auto-generated if not specified)
# certificate_key: ""

# Container registry credentials (if using private registry)
# registry_url: "registry.example.com"
# registry_username: "admin"
# registry_password: "password"

# Optional: Encrypt this file with ansible-vault
# ansible-vault encrypt secrets.yml
