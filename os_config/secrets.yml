---
# Example secrets file for os_config playbook
# Replace the example password with a secure value before use

# To create an encrypted secrets file with Ansible Vault, use:
#   ansible-vault create secrets.yml
# Or to encrypt an existing file:
#   ansible-vault encrypt secrets.yml
# To edit an encrypted file:
#   ansible-vault edit secrets.yml
# For more info, see: https://docs.ansible.com/ansible/latest/vault_guide/index.html

devops_password: password
